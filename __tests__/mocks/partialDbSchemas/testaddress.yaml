$id: lattice.core:__tests__.partials.testaddress
type: object
properties:
  address: {type: string, maxLength: 128}
  location:
    type: object
    required: [type, coordinates]
    properties:
      type: { type: string,  const: point}
      coordinates:
        type: array
        maxItems: 2
        minItems: 2
        items: [
            { type: number, min: -180, max: 180 },
            { type: number, min: -90, max: 90 }
        ]
